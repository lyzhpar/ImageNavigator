<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/editorRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 🟦 Sidebar -->
    <LinearLayout
        android:id="@+id/sidebarContainer"
        android:layout_width="270dp"
        android:layout_height="0dp"
        android:background="#F2F2F2"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <include
            android:id="@+id/headerAdventure"
            layout="@layout/adventure_header_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Miniatures -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewThumbnails"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:padding="4dp"
            android:scrollbars="vertical" />
    </LinearLayout>

    <!-- 🟥 Zone de dessin -->
    <com.example.imagenavigator.screens.DrawingView
        android:id="@+id/drawingView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#FFFFFF"
        android:elevation="1dp"
        app:layout_constraintBottom_toTopOf="@id/incomingLinksContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/sidebarContainer"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 🟨 Bande incomingLinks entre DrawingView et BottomBar -->
    <FrameLayout
        android:id="@+id/incomingLinksContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="0dp"
        android:background="#EEEEEE"
        android:visibility="visible"
        android:elevation="1dp"
        app:layout_constraintTop_toBottomOf="@id/drawingView"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintStart_toEndOf="@id/sidebarContainer"
        app:layout_constraintEnd_toEndOf="parent"
        >

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/incomingLinksRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:clipToPadding="false"
            android:scrollbars="horizontal" />

    </FrameLayout>

    <!-- 🟦 Delete Zones Button -->
    <ImageButton
        android:id="@+id/deleteZonesButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:backgroundTint="@color/design_default_color_error"
        android:src="@android:drawable/ic_menu_delete"
        android:translationZ="10dp"
        android:contentDescription="Supprimer les zones"
        android:visibility="gone"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ⏳ ProgressBar -->
        <ProgressBar
        android:id="@+id/loadingOverlay"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:visibility="gone"
        android:indeterminate="true"
        android:elevation="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <FrameLayout
        android:id="@+id/syncOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FF000000"
        android:visibility="gone"
        android:elevation="10dp"
        android:clickable="true"
        android:focusable="true">


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Synchro !..."
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_gravity="center" />
    </FrameLayout>

    <!-- 🟩 BottomBar -->
    <include
        android:id="@+id/bottomBar"
        layout="@layout/bottom_bar_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:elevation="4dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:viewBindingIgnore="false" />





</androidx.constraintlayout.widget.ConstraintLayout>